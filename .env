HOST=0.0.0.0
ENV_NAME = development
PORT=5015

POSTGRES_HOST=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB_NAME=geminidb
POSTGRES_DB_PORT=5432

# RABBIT_MQ
RABBITMQ_ENABLED=false

# ============== Logger Options ==============
LOG_LEVEL=debug

# Victoria Logs configuration
OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://localhost:9428/insert/opentelemetry/v1/logs # This key is required only when using [USE_VICTORIA] is set to true
OTEL_NODE_RESOURCE_DETECTORS=service # Used for filter the [tags] on victoria logs and display only the ones that start with [service]

